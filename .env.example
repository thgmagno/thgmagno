# Token de leitura do GitHub (opcional).
GITHUB_TOKEN=

# Segredo do webhook do GitHub (opcional).
# Quando definido permite atualizar o cache do site via POST /api/github/revalidate
# configurado em Settings › Webhooks no GitHub.
GITHUB_WEBHOOK_SECRET=
